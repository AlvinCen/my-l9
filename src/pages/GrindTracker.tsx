import React from 'react';
import ComingSoon from '../components/ComingSoon';

const GrindTracker: React.FC = () => {
  return (
    <ComingSoon
      title="Grind Tracker (Under Construction)"
      description="We are repurposing this feature into a comprehensive CP Simulator."
      hint="Your previous grind data is safe, but the interface is being updated to support the new CP Simulation features."
    />
  );
};

export default GrindTracker;